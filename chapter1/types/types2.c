// Create an integer variable, assign a value, and print
#include <stdio.h>

int main(void)
{
    // Initalize an integer variable called x with the value 13
    int x = 13;

    // Initialize and integer variable called y with the value 67
    int y = 67;

    // Print out the current value of x
    printf("The current value of x is %i and the current value of y is %i.\n", x, y);
}
