/**
 * Cash
 *
 * CS50 AP
 * Name
 * Date
 *
 */

 #include <stdio.h>

 int main(void)
 {
    // Get the change from user, be sure the user doesn't provide a negative

    do
    {

    }
    while()

    // Initialize a coins counter to 0

    // Determine the number of quarters needed
    while(change >= .25)
    {
        // Subtract .25 from change

        // Update the coins counter by 1
    }

    // Determine the number of dimes

    // Determine the number of nickles

    // Determine the number pennies

    // Print result
 }
